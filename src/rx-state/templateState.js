const templateState = {
    todos: {
        toggleAll: false,
        newTodoValue: '',
        items: [
            //{label: 'test', checked: false, restoreLabel: ''},
        ]
    }
};

export default templateState;
