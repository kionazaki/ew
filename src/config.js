const config = {
    RootElementId: 'easy_web_root', // ID root-элемента
    iframeSrc: 'http://kionazaki.github.io/tmp/iframe.html', // SRC для скрытого IFRAME (для получения токена)
    tokenTimeout: 5000, // Таймаут при ожидании токена
};

export default config;
